# llms.txt for cargo-commitlint

# Project Information
Name: cargo-commitlint
Description: A Rust-based commit message linter following the Conventional Commits specification
Repository: https://github.com/quinnjr/cargo-commitlint
License: MIT OR Apache-2.0
Language: Rust

# Key Features
- Validates commit messages against Conventional Commits specification
- Configurable via TOML (similar to commitlint)
- Git hook installation via the built-in installer or build script
- Supports all standard Conventional Commit types
- Customizable rules for type, scope, subject, body, and footer
- Regex-based commit message parsing
- Ignore patterns for skipping validation

# Installation
From Source:
  git clone https://github.com/quinnjr/cargo-commitlint.git
  cd cargo-commitlint
  cargo install --path .

From Crates.io:
  cargo install cargo-commitlint

# Usage
Install git hook:
  cargo commitlint install

Validate commit message:
  cargo commitlint check --message "feat: add new feature"

# Configuration
Configuration file: commitlint.toml or .commitlint.toml
Format: TOML

Example configuration:
  [rules]
  [rules.type]
  enum = ["feat", "fix", "docs", "style", "refactor", "test", "chore"]
  case = "lowercase"

  [rules.scope]
  enum = []
  case = "lowercase"

  subject_case = ["sentence-case"]
  header_max_length = 72

# Conventional Commit Types
- feat: A new feature
- fix: A bug fix
- docs: Documentation only changes
- style: Changes that do not affect the meaning of the code
- refactor: A code change that neither fixes a bug nor adds a feature
- perf: A code change that improves performance
- test: Adding missing tests or correcting existing tests
- build: Changes that affect the build system or external dependencies
- ci: Changes to CI configuration files and scripts
- chore: Other changes that don't modify src or test files
- revert: Reverts a previous commit

# Git Hooks
Pre-commit: Runs cargo fmt --check and cargo clippy
Pre-push: Runs cargo test
Commit-msg: Validates commit messages using cargo-commitlint

# Integration
- Installs its own commit-msg hook; use [package.metadata.commitlint] to configure
- Designed for Rust projects
- Compatible with Conventional Commits specification

# Documentation
Full documentation available at: https://quinnjr.github.io/cargo-commitlint/

# Contributing
Contributions welcome! Please follow Conventional Commits for commit messages.

# Keywords
rust, commitlint, conventional commits, git hooks, cargo, rust tooling, commit message validation, code quality, developer tools

